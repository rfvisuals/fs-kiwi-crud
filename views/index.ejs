<!DOCTYPE html>
<html>
  <head>
    <% include ./partials/head.ejs %>
    <!-- native css directory -->
    <link rel="stylesheet" href="/css/app.css"> 
  </head>
    <body>
    <h1>Steak List</h1>
      <h2>Add Steaks to Your List<h2>
        
      <a href="steaks/new" method="GET"class="waves-effect waves-light btn">Add New Steak</a>
              <ul>
      <% for(let i = 0; i < steaks.length; i++){ %>
          <li>
             <%= steaks[i].restaurant %>
             <a href="/steaks/<%=steaks[i].id;%>"><%=steaks[i].steakname;%> </a> cooked <%=steaks[i].doneness; %>  is a <%=steaks[i].rating; %> out of 10.
              <form action="/steaks/<%=steaks[i].id; %>?_method=DELETE" method="POST">
                <input type='submit' value='DELETE'/>
              </form>
              <a href="/steaks/<%=steaks[i].id; %>/edit">Edit</a>
          </li>
      <% } %>
  </ul>
    </body>
</html>